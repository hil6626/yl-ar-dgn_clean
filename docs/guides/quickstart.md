# 快速开始指南

**版本:** 1.0.0  
**最后更新:** 2026-02-16  
**预计时间:** 5分钟

---

## 🎯 目标

在5分钟内启动 YL-AR-DGN 项目的所有核心服务。

---

## 📋 前置条件

### 系统要求
- **操作系统**: Ubuntu 20.04+ 或 Windows 10+
- **Python**: 3.8 或更高版本
- **内存**: 至少 8GB RAM
- **磁盘空间**: 至少 20GB 可用空间

### 已安装的依赖
确保以下Python包已安装：
```bash
pip install fastapi uvicorn flask pyqt5 opencv-python numpy requests psutil python-socketio
```

---

## 🚀 快速启动步骤

### 步骤1: 进入项目目录

```bash
cd /home/vboxuser/桌面/项目部署/项目1/yl-ar-dgn_clean
```

### 步骤2: 验证所有服务状态

使用统一入口脚本检查当前状态：
```bash
./scripts/yl-ar-dgn.sh status
```

预期输出：
```
YL-AR-DGN 项目状态
============================================================
组件状态:
----------------------------------------
✓ YL-monitor: running (PID: 174292, 端口: 5500)
✓ AR-backend: running (PID: 174752, 端口: 5501)
✓ User GUI: running (PID: 174753, 端口: 5502)
```

### 步骤3: 启动服务（如未运行）

如果服务未运行，使用以下命令启动：

```bash
# 启动所有服务
./scripts/yl-ar-dgn.sh start

# 或分别启动
python YL-monitor/start_server.py &    # 端口5500
python AR-backend/monitor_server.py &  # 端口5501
cd user && python3 main.py &           # 端口5502
```

### 步骤4: 验证服务健康

```bash
# 验证 YL-monitor
curl http://0.0.0.0:5500/api/health
# 预期: {"status": "healthy", "service": "yl-monitor"}

# 验证 AR-backend
curl http://0.0.0.0:5501/health
# 预期: {"status": "healthy", "service": "ar-backend"}

# 验证 User GUI
curl http://0.0.0.0:5502/health
# 预期: {"status": "healthy", "service": "user-gui"}
```

### 步骤5: 访问监控面板

打开浏览器，访问：
```
http://0.0.0.0:5500
```

### 步骤6: 启动 User GUI 界面

```bash
cd user
python3 main.py
```

这将打开 PyQt5 图形界面，您可以：
- 配置视频源
- 选择人脸模型
- 调整音频效果
- 查看实时预览

---

## ✅ 验证清单

| 检查项 | 命令 | 预期结果 |
|--------|------|----------|
| YL-monitor 运行 | `curl http://0.0.0.0:5500/api/health` | 返回 healthy |
| AR-backend 运行 | `curl http://0.0.0.0:5501/health` | 返回 healthy |
| User GUI 运行 | `curl http://0.0.0.0:5502/health` | 返回 healthy |
| 监控面板可访问 | 浏览器访问 `http://0.0.0.0:5500` | 页面正常显示 |
| GUI 界面可启动 | `python3 user/main.py` | 窗口正常显示 |

---

## 🆘 常见问题

### 问题1: 端口被占用

**症状**: 服务启动失败，提示端口已被使用

**解决**:
```bash
# 查找占用端口的进程
netstat -tlnp | grep 5500

# 停止占用进程
kill -9 <PID>

# 或使用备用端口（修改配置文件）
```

### 问题2: 模块导入错误

**症状**: `ImportError: No module named 'xxx'`

**解决**:
```bash
# 安装缺失的依赖
pip install <module-name>

# 或安装所有依赖
pip install -r requirements.txt
```

### 问题3: GUI 无法启动

**症状**: `python3 user/main.py` 无响应或报错

**解决**:
```bash
# 检查 PyQt5 安装
pip install PyQt5

# 检查 DISPLAY 环境变量（Linux）
echo $DISPLAY
# 如为空，设置: export DISPLAY=:0

# 检查 X11 转发（远程连接时）
```

---

## 📚 下一步

- [故障排查指南](troubleshooting.md) - 详细问题解决方法
- [常见问题](faq.md) - 更多FAQ
- [部署索引](../../部署/0.部署索引.md) - 完整部署文档

---

## 🎉 恭喜！

您已成功启动 YL-AR-DGN 项目！现在可以：
- 通过浏览器监控所有服务状态
- 使用 GUI 进行 AR 合成操作
- 查看实时视频处理效果
